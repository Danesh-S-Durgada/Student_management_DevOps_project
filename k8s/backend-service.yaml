apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: capstone
  labels:
    app: backend
    env: dev
    project: capstone
spec:
  selector:
    app: backend
    env: dev
    project: capstone
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
